<!--退出活动-->
<!-- 查询现有关系，向Relationships控制器的destroy动作发送DELETE请求，销毁此关系-->
<%= form_for(current_user.active_relationships.find_by(followed_act_id: @activity.id), html: { method: :delete }) do |f| %>
  <%= f.submit "退出活动", class: "btn" %>
<% end%>